# Install zsh and oh-my-zsh for Ubuntu 18.04

## Install zsh

* Install **zsh**:

  ```
  $ sudo apt-get update
  $ sudo apt-get install zsh
  ```

* Use **zsh** as default:

  ```
  $ sudo chsh -s /bin/zsh
  ```

* Restart shell
